# Notes Vault — Claude Context

Obsidian-based personal knowledge base, task tracker, and life hub. Maintain obsidian-compatible notes, but don't use `[[]]`, instead use markdown links []() that will resolve even outside obsidian. 

Claude maintains everything it needs for itself in the `/claude` directory. Update it when needed, save reflections for itself.